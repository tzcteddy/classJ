1 变量提前声明
    var 只声明但是没有赋值，浏览器默认赋值undefined，当代码执行到这一行才赋值
    function 既声明又赋值，当代码执行到这一行直接跳过
2 特殊情况
    1）不看等号右边
    2）不看条件
    3）自执行函数名
    4）函数和变量重名
    5）return下面
3 私有变量 函数体内定义的变量、形参
  全局变量 没有任何函数包含的变量
        ps: 全局变量都是window的一个属性
        ps: 取值如果自己没有那么一定会向上找，当找到全局还没有。报错
        ps: 赋值如果自己没有(私有不存在)向上找，如果到全局还没有。给window
4 引用数据类型赋值过程，函数赋值过程，共同引用